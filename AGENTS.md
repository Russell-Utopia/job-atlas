# Project instructions

- 使用中文沟通。
- 先完成设计与确认；仅在用户明确要求实施后编写业务代码。

## Agent skills

### Issue tracker

本仓库使用 GitHub Issues 管理议题；不要在 `.scratch/` 下创建重复票据。参见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用五个默认 triage 标签。参见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单领域文档布局。参见 `docs/agents/domain.md`。
